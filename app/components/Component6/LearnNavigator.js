import React, {Component} from "react";
import {AppRegistry, View, Text, Navigator} from "react-native";

export default class LearnNaviagtor extends Component {
    render(){
        return(
            <View>
                
            </View>
        );
    }
}

AppRegistry.registerComponent('LearnNavigator', () => LearnNaviagtor);